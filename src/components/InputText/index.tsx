import React, { FC } from 'react';
import { TextInput as Input, View } from 'react-native';

const TextInput: FC = () => {
  return (
    <View>
      <Input />
    </View>
  );
};

export default TextInput;
